<p id="notice"><%= notice %></p>

<h1>Benachrichtigungen</h1> <%= link_to 'Neue User Benachrichtigung', new_anotification_path %>

<table class="table table-bordered">
  <thead>
    <tr>
      <th>Inhalt</th>
      <th colspan="4"></th>
    </tr>
  </thead>

  <tbody>
    <%if admin_notifications.present? %>
      <% admin_notifications.each do |admin_notification| %>
        <%unless admin_notification.users.include?(current_user) %>
          <tr>
            <td><%= admin_notification.body %></td>
            <td><%= link_to 'Gelesen von', read_by_path(admin_notification.id) %></td>
            <td><%= link_to 'Bearbeiten', edit_anotification_path(admin_notification) %></td>
            <td><%= link_to 'Löschen', admin_notification, method: :delete, data: { confirm: 'Bist du sicher?' } %></td>
          </tr>
        <%end%>
      <% end %>
    <%end%>
  </tbody>
</table>
